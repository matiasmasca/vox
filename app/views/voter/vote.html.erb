<!-- Infor Proceso -->
<div>
	<div class="row">
		<%= render partial: "voter/selection_process_public_info", object: @selection_process %>
	</div><!-- /.row -->
</div>
<!-- Categoria -->
<div>
	<%= render partial: "voter/categories_buttons", object: @selection_process %>
</div>
<!-- Candidatos categoría seleccionada -->

<%= form_for @ballot,:html => { :class => 'form-horizontal', :role => 'form' } do |f| %>
  <% if @ballot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ballot.errors.count, "error") %> prohibited this ballot from being saved:</h2>

      <ul>
      <% @ballot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%= f.hidden_field :selection_process_id, value: @selection_process.id %> 
<%= f.hidden_field :category_id, value: @category.id unless @category.nil? %> 
<div>
  <div class="row">
<% unless @category.nil? %>
  <% unless @category.already_vote?(current_user.id) %>
  <div>
    <div class="input-group">
	<% @category.candidate.each do |candidate| 
      @candidate = candidate       
	 %>  
	<% unless @candidate.nil? %>
	<div class="col-sm-6 col-md-4">
        <div class="thumbnail">
           <%= link_to(image_tag(@candidate.image_path,
        	size: "242x200",
        	alt: "Avatar #{@candidate.id}",
        	id: "imgAvatar-#{@candidate.id}",
        	class: "img-responsive"
        	), 
        	@candidate.image_path) %>
            <div class="caption">
	            <h3><%= @candidate.name %></h3>
	            <div style="white-space: pre-line; text-align: justify; ">
	              <p><%= @candidate.bios %></p>
	            </div>
	            <p><%= link_to ' Info', category_candidate_path(@candidate.category_id, @candidate), :class => 'btn btn-default btn glyphicon glyphicon-plus', :role => "button" %>
	          </div><!-- caption -->
	           <div  class="well"> 
	        	    <%= f.radio_button :candidate_id, "#{candidate.id}" %>
  					<%= label :candidate_name, "#{candidate.name}" %>

        	   </div>
        </div><!-- thumbnail --> 

	</div><!-- col-sm-6 -->

	<% end %><!-- candidato -->
	<%  end %><!-- ya voto en categoria -->
	<%  end %><!-- candidatos categoria -->
      
	</div><!-- /input-group -->
  </div><!-- /.col-lg-6 -->
<%  end %><!-- /category -->
  </div><!-- /.row -->
</div>
<!-- registrar voto -->
<div class="actions">
 <%= f.submit ' Votar seleccionado', data: { confirm: "Esta seguro que desea Votar el candidato seleccionado? NO podrá cambiar su elección una vez precionado el botón Aceptar." },  class: 'btn btn-success btn glyphicon glyphicon-envelope' %>
</div>
<% end %>
<hr>
<div class="row">
Comparte: 

<% texto_adicional = "Vos también podes votar en #{@selection_process.name_process} por tu favorito, como yo lo hice." %>

<!--Social Share -->
<!--Facebook -->
<a class="btn btn-default" href="http://www.facebook.com/sharer.php?u=<%= request.original_url %>&t=<%= texto_adicional %>"><img class="img" src="https://fbstatic-a.akamaihd.net/rsrc.php/v2/yQ/r/7GFXgco-uzw.png" alt="" style="top: 0px;" height="14" width="14"> Facebook</a>

<!--Twitter -->
<a href="https://twitter.com/share" class="twitter-share-button" data-counturl="<%= request.original_url %>" data-text="<%= texto_adicional %>" data-related="matiasmasca" data-lang="es" data-size="large" data-count="none" data-via="TFA-VOX" >Tweet</a>
<script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0];if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src="https://platform.twitter.com/widgets.js";fjs.parentNode.insertBefore(js,fjs);}}(document,"script","twitter-wjs");</script>

<!--Google plus -->
<a class="btn btn-default" href="https://plus.google.com/share?url=<%= request.original_url %>"><img src="https://www.gstatic.com/images/icons/gplus-16.png" alt="Share on Google+"/> Google Plus</a>

</div>