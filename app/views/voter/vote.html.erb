<!-- Infor Proceso -->
<div>
	<div class="row">
		<%= render partial: "voter/selection_process_public_info", object: @selection_process %>
	</div><!-- /.row -->
</div>
<!-- Categoria -->
<div>
	<%= render partial: "voter/categories_buttons", object: @selection_process %>
</div>
<!-- Candidatos categoría seleccionada -->
<%= form_tag({action: :vote}) do %>
<div>
  <div class="row">
	<%= render partial: "voter/candidates_category_choice", object: @category %>
  </div><!-- /.row -->
</div>
<!-- registrar voto -->
<div>
<%= link_to ' Votar seleccionado', '/', :class => 'btn btn-primary btn glyphicon glyphicon-envelope', :role => "submit" %>
</div>

<% end %>