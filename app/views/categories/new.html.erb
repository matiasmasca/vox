<h1>Nueva categoria</h1>
<p>Las categorias, forman las n-uplas de los procesos (por ejemplo una terna de nominados, tiene 3 plazas.

<%= render 'form' %>

<%= link_to ' Ir a Categorias', categories_path, :class => 'btn btn-default btn glyphicon glyphicon-list' %>
